ML-78 Privacy / App Store Review Readiness v1 Handoff

Summary
- Added centralized public privacy/support link config in src/lib/app-links.ts.
- Updated Settings legal UI to show "Privacy & Support" for both guest and
  authenticated users.
- Privacy Policy opens the production Notion policy URL.
- Support opens mailto:asrazdorskiy@gmail.com.
- Link-open failures now render generic inline copy and log only a generic
  message without raw thrown error details.
- Added privacy/App Store owner docs and ML-78 QA coverage.
- Bumped the app patch version from 1.18.2 to 1.18.3.

Files changed
- AGENTS.md
- package.json
- package-lock.json
- docs/app-store-metadata.md
- docs/app-store-privacy-checklist.md
- docs/app-store-screenshots.md
- docs/mvp-qa-checklist.md
- docs/privacy-policy-update-draft.md
- docs/handoffs/ML_78_HANDOFF.txt
- src/lib/app-links.ts
- src/features/settings/settings-screen.tsx
- src/features/settings/__tests__/settings-screen.test.ts

Validation results
- PASS targeted Settings test:
  npm test -- --runInBand src/features/settings/__tests__/settings-screen.test.ts
- PASS npm run release:preflight
- PASS npm test -- --runInBand
- PASS npm run typecheck
- PASS npm run lint
- PASS npm run format:check
- PASS npx expo config --json
- PASS git diff --check
- PASS targeted regression checks:
  CSV v1 header remains exactly
  id,amount,category,isLeak,leakReason,note,createdAt.
  Bottom tabs remain Home, Analytics & Leaks, and Settings.
  Add Transaction and Shame Card remain pushed root Stack screens.
  No service-role/admin Supabase usage was added to mobile app runtime
  code/config/package files.
  No dynamic process.env[...] access exists in src or app.
  Settings/legal UI raw-value safety is covered by Settings tests.
  No @expo/ui, @expo/ui-swift-ui, BlurView, expo-blur, Liquid Glass, or glass
  styling was added.

Manual QA for owner
- Verify the Privacy Policy URL opens publicly without a Notion login:
  https://www.notion.so/quitesocial/35357a24e62c804dab18c28d24a6c75a?showMoveTo=true&saveParent=true
- In guest/local mode, open Settings and confirm Privacy & Support, Privacy
  Policy, and Support are visible without forcing sign-in.
- Sign in and confirm Privacy & Support remains visible.
- Tap Privacy Policy and confirm the configured Notion URL opens.
- Tap Support and confirm a mail client opens for asrazdorskiy@gmail.com.
- Force a link-open failure and confirm Settings shows only
  "Couldn't open this link right now." with no raw technical details.
- Tap Sign Out and confirm local data remains on device.
- Run Delete Account through the existing confirmation flow and confirm cloud
  account deletion/sign-out behavior while local data remains on device.
- Recheck Backup, Restore, Sync, CSV import/export, bottom tabs, Add
  Transaction, Shame Card, and local guest mode.

Preserved invariants
- Guest/local mode remains permanently available; no login wall was added.
- Local tracking remains local-first and offline-capable.
- No sync triggers were added.
- No sync on cold start, login, session restore, sign out, background
  execution, or transaction/category add/edit/delete.
- Foreground auto sync remains limited to AppState foreground returns,
  throttled by last successful sync metadata, and routed through the existing
  sync service boundary.
- Settings sync UI remains routed through manualSyncService and sync metadata
  boundaries.
- Sign Out remains separate from Delete Account and does not delete local data.
- Delete Account still uses the existing delete account service path and then
  signs out; local SQLite data remains on device.
- CSV v1 remains exactly
  id,amount,category,isLeak,leakReason,note,createdAt.
- Bottom tabs remain Home, Analytics & Leaks, and Settings.
- Add Transaction and Shame Card remain pushed root Stack screens.
- app.config.js and eas.json were not changed.

Version bump
- 1.18.2 -> 1.18.3.
- This is a patch bump because ML-78 adds docs and a small Settings
  privacy/support UI hardening pass without changing core app behavior.
- package.json remains the source of truth for version, and app.config.js
  continues to read package.json.version.

What was not done
- The existing Notion privacy policy was not edited automatically.
- docs/privacy-policy.md was not rewritten or treated as the live replacement
  policy.
- No new sync/backend/account functionality was added.
- No tracking, advertising, no-sale, local DB encryption, or end-to-end
  encryption guarantees were added.
- No new dependencies were added.
- No CSV, navigation, auth, backup, restore, sync, or Delete Account behavior
  was redesigned.

Owner follow-up
- Manually copy owner-approved blocks from docs/privacy-policy-update-draft.md
  into the existing Notion privacy policy.
- Complete docs/app-store-privacy-checklist.md before App Store submission.

AGENTS.md update
- Added one durable rule because legal/support links and safe link failure copy
  are project conventions likely to matter in future Settings/release work.

Diff:
@@ -59,6 +59,7 @@ nvm use 20.19.4
 - Keep foreground auto-sync limited to React Native `AppState` foreground returns, throttled by last successful sync metadata, and routed through the existing sync service boundary.
 - Keep user-facing Settings sync UI routed through the manual sync service and sync metadata boundaries; do not call remote sync adapters or Supabase clients directly from screen code.
 - Keep sync attempt source metadata limited to the safe enum values `manual` and `foreground`; do not store it inside aggregate summaries or raw diagnostics.
+- Keep public legal/support links centralized in `src/lib/app-links.ts`; Settings legal/support link failures should show generic safe copy without raw technical errors.
