ML-73 Manual Sync Hardening & Cross-Screen Refresh v1 Handoff

Summary
- Hardened manual incremental sync while keeping sync manual-only from
  Settings.
- Bumped the app patch version from 1.17.0 to 1.17.1.
- No navigation, CSV, backup/restore, Delete Account, auth lifecycle, or
  automatic sync behavior was added.

Implemented behavior
- Settings uses an immediate in-flight ref guard so fast repeated Sync now
  presses cannot call the manual sync service twice before disabled state
  renders.
- createSyncService shares one in-flight incremental sync promise per service
  instance, so direct overlapping calls do not start duplicate pull/apply/push
  or success metadata writes.
- After a successful manual sync, Settings refreshes local transaction and
  category stores so pulled remote changes are visible without app restart.
- Failed, skipped, or thrown manual sync attempts show only
  "Couldn't sync. Try again." and do not run the success-only store refresh.
- Settings can display the last persisted safe aggregate sync summary after
  reload: pulled, pushed, applied, conflicts, and ignored.
- Missing, invalid, non-finite, negative, or corrupted persisted sync summary
  values are ignored safely and do not render raw values.

Files changed
- src/features/settings/settings-screen.tsx
- src/features/settings/__tests__/settings-screen.test.ts
- src/lib/sync/sync-service.ts
- src/lib/sync/__tests__/sync-service.test.ts
- docs/mvp-qa-checklist.md
- docs/handoffs/ML_73_HANDOFF.txt
- package.json
- package-lock.json

Preserved invariants
- Sync remains behind featureFlags.incrementalSyncEnabled.
- Sync remains manual-only through Settings -> Sync now.
- Guest/local mode still has no Sync card and cannot trigger sync.
- No sync was added on app start, login, session restore, sign out,
  background, or local transaction/category mutation.
- No conflict UI was added.
- No category tombstone semantics were added.
- CSV v1 remains unchanged.
- Bottom tabs remain Home, Analytics & Leaks, and Settings.
- Add Transaction and Shame Card remain pushed root Stack screens.
- Backup, restore, Delete Account, import/export, and auth behavior remain
  unchanged.
- Mobile app code/config still does not use Supabase service-role/admin clients
  or keys.
- Settings sync UI does not render raw backend errors, env values, Supabase
  URLs, anon keys, service-role keys, OAuth/provider secrets, access tokens,
  refresh tokens, provider tokens, Apple identity tokens, localOwnerId,
  deviceId, ownerId, raw user IDs, or row payloads.
- No @expo/ui, @expo/ui-swift-ui, BlurView, expo-blur, Liquid Glass, or glass
  styling was added.

Validation run
- PASS npm run release:preflight
  - 9 checks passed, 0 failed.
  - Expo version resolves to 1.17.1.
- PASS npm test -- --runInBand
  - 38 test suites passed.
  - 290 tests passed.
- PASS npm run typecheck
- PASS npm run lint
- PASS npm run format:check
- PASS npx expo config --json
  - Resolved Expo version is 1.17.1.
- PASS git diff --check

Manual QA checklist
- In guest/local mode, open Settings and confirm the Sync card is hidden.
- Sign in with Google, open Settings, and confirm the Sync card is visible.
- Tap Sync now once and confirm the button changes to Syncing..., then returns
  to Sync now.
- Fast double tap Sync now and confirm only one sync operation runs.
- After a successful sync that pulls remote transaction/category changes,
  confirm Home, Analytics, transaction forms, and category-dependent UI show the
  fresh local data without app restart.
- Force a recoverable sync failure or missing session and confirm Settings
  shows only "Couldn't sync. Try again." with no success summary from that
  attempt.
- Reload Settings after a successful sync and confirm only aggregate summary
  counts appear when valid metadata exists.
- Tap Sign Out and confirm sync does not run and local data remains visible
  after returning to guest/local mode.
- Run manual backup, restore, and Delete Account flows and confirm they do not
  trigger sync.

Explicit non-goals
- No automatic sync.
- No background sync.
- No sync on login/session restore/app start/sign out/local mutation.
- No conflict UI.
- No category tombstone implementation.
- No backup/restore redesign.
- No Delete Account changes.
- No CSV v2.
- No navigation changes.
- No visual redesign.
- No new dependencies.
