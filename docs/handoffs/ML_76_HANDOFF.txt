ML-76 Sync Status & Cross-device QA Hardening v1 Handoff

Summary
- Made the authenticated Settings Sync card explicit about foreground auto
  sync by showing "Auto sync: On" and the return-to-app/offline tracking copy.
- Kept the card authenticated-only and kept manual "Sync now" available with
  the existing "Syncing..." loading label.
- Strengthened Settings sync tests around safe aggregate metadata rendering,
  corrupted metadata handling, manual sync invocation, and raw diagnostic
  suppression.
- Bumped the app patch version from 1.18.0 to 1.18.1 because this is sync
  UX/status hardening around the already shipped ML-75 foreground sync.

What changed
- Updated src/features/settings/settings-screen.tsx Sync card copy only.
- Updated src/features/settings/__tests__/settings-screen.test.ts to verify
  auto-sync copy and broader raw env/token/provider/backend/id value safety.
- Added the ML-76 QA section to docs/mvp-qa-checklist.md.
- Updated package.json and package-lock.json version fields to 1.18.1.

Files changed
- src/features/settings/settings-screen.tsx
- src/features/settings/__tests__/settings-screen.test.ts
- docs/mvp-qa-checklist.md
- docs/handoffs/ML_76_HANDOFF.txt
- package.json
- package-lock.json

Intentionally not changed
- No manual/foreground sync attempt type was persisted because the current
  sync service boundary does not carry attempt type, and adding it would be
  broader than this UX/status hardening task.
- No new sync triggers were added.
- No sync on cold start, login, session restore, sign out, background
  execution, or transaction/category add/edit/delete.
- No SyncService, SyncMetadata, SyncSummary, remote adapter, auth provider,
  backup/restore, Delete Account, navigation, route, or CSV format changes.
- No direct Supabase calls were added from Settings or root lifecycle code.
- No new dependencies, conflict UI, category tombstones, raw diagnostics UI,
  @expo/ui, BlurView, expo-blur, Liquid Glass, or glass styling.
- AGENTS.md was not updated because its existing sync trigger, Settings
  boundary, service-role, foreground auto-sync, and validation rules already
  cover the durable ML-76 constraints.

Validation
- PASS targeted Settings test:
  npm test -- --runInBand src/features/settings/__tests__/settings-screen.test.ts
- PASS npm run release:preflight
- PASS npm test -- --runInBand
- PASS npm run typecheck
- PASS npm run lint
- PASS npm run format:check
- PASS npx expo config --json
- PASS git diff --check
- PASS targeted regressions: CSV v1 header, bottom tab labels, pushed
  Add Transaction / Shame Card routes, no service-role/admin mobile usage, no
  dynamic process.env[...] access in src/app, Settings raw-value safety, and
  no @expo/ui / BlurView / expo-blur / Liquid Glass additions.

Manual QA checklist
- In guest/local mode, open Settings and confirm the Sync card is hidden.
- Sign in with Google, open Settings, and confirm the Sync card shows
  "Auto sync: On", "Runs when you return to the app. Local tracking still
  works offline.", and "Sync now".
- Tap Sync now and confirm the button changes to "Syncing...", then returns to
  "Sync now".
- After a successful sync, reload Settings and confirm Last sync plus only
  aggregate counts are shown.
- On two signed-in devices or simulators, create or edit data on device A,
  sync device A, then return device B to the foreground after the throttle
  window or tap Sync now and confirm remote changes appear.
- Delete a transaction on device A, sync device A, then sync or foreground
  device B and confirm the transaction is hidden locally on device B.
- Force a recoverable sync failure and confirm Settings shows only
  "Couldn't sync. Try again." with no raw diagnostics.
- Background/foreground within 15 minutes of the last successful sync and
  confirm no repeated foreground sync batch is observed.
- Add, edit, and delete local transactions/categories and confirm those local
  mutations do not trigger sync until manual sync or a later eligible
  foreground return.
- Recheck CSV import/export, bottom tabs, Add Transaction, Shame Card, Backup,
  Restore, Delete Account, Sign Out, and local guest mode.

Preserved invariants
- Guest/local mode remains permanently available; no login wall was added.
- Foreground auto sync remains limited to AppState foreground returns,
  throttled by last successful sync metadata, and routed through the existing
  sync service boundary.
- Manual Settings Sync now remains authenticated-only and unthrottled by the
  foreground 15-minute window.
- Settings sync UI remains safe and does not render raw env values, Supabase
  URLs, anon keys, service-role keys, OAuth/provider secrets, access tokens,
  refresh tokens, provider tokens, Apple identity tokens, localOwnerId,
  deviceId, ownerId, raw user IDs, row payloads, or raw backend errors.
- CSV v1 header remains exactly
  id,amount,category,isLeak,leakReason,note,createdAt.
- Bottom tabs remain Home, Analytics & Leaks, and Settings.
- Add Transaction and Shame Card remain pushed root Stack screens.
- package.json remains the version source of truth, and app.config.js still
  reads package.json.version.

Next-agent notes
- Owner manual QA should still verify real/simulator cross-device sync.
- If future work needs to show whether the last sync was manual or foreground,
  add attempt type deliberately at the sync service/metadata boundary instead
  of inferring it in Settings UI.
